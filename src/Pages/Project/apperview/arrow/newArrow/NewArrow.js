import React from 'react';
import './NewArrow.css'

const NewArrow = ({  }) => {
    return (
        <button className='new-arrow'>
           ← connect
        </button>
    );
};

export default NewArrow;